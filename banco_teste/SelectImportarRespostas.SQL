SELECT COD_USUARIO, NOME, AREA, MICRO_AREA, UNIDADE, COD_PROF, CNS_PROF, CPF_PROF, CBO_PROF, COD_INE
FROM USUARIO;


SELECT COD_DOMICILIO, TIPO_LOGRADOURO || ' ' || LOGRADOURO || ', ' || NUMERO AS ENDERECO, BAIRRO, VISITADO
FROM DOMICILIO_VISITA;


SELECT *
FROM ESUS_FICHA_VISITA_DOM F
    INNER JOIN FVD_MOTIVO_VISITA MV ON (F._ID = MV._ID_FICHA_VISITA_DOM)
    

SELECT * FROM ESUS_FICHA_VISITA_DOM
WHERE COD_DOMICILIO = 1779;

SELECT * FROM FVD_MOTIVO_VISITA
WHERE _ID_FICHA_VISITA_DOM = 1;
